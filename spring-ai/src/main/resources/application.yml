spring:
  application:
    name: llmentor

server:
  port: 8080

dashscope:
  api-key: ${DASHSCOPE_API_KEY:}
  api-url: https://dashscope.aliyuncs.com/compatible-mode/v1/chat/completions
  model: qwen-plus
